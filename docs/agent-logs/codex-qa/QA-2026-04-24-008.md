# QA-2026-04-24-008

## Target
- Task ID: `SEMI-TASK-003`
- Requirement ID: `SEMI-REQ-003`
- Branch: `feature/SEMI-TASK-003-issue-domain-model`
- Reviewed commits:
  - `5073e80`
  - `253fa43`
- Date: `2026-04-24`
- Reviewer: `Codex QA`

## Findings
1. `Major` The Pydantic input schema does not enforce the required `title` length limit.
   - Files:
     - `requirements/SEMI-REQ-003-issue-domain-model.md:15`
     - `app/schemas/issue.py:9`
   - Detail:
     - the requirement explicitly says `title` is `max 255`
     - `IssueCreate.title` is declared as plain `str`, so titles longer than 255 characters are accepted by schema validation
   - Impact:
     - API-layer validation will accept invalid payloads and only fail later at the persistence layer
     - this violates the requirement and weakens the stated contract that Pydantic schemas validate input/output

2. `Major` The task branch includes an unrelated QA artifact from Phase 1.
   - File: `docs/agent-logs/codex-qa/QA-2026-04-24-007.md`
   - Evidence:
     - the file appears in `git diff --name-status main..HEAD`
     - it is outside the scope defined in `tasks/SEMI-TASK-003-issue-domain-model.md:14`
   - Impact:
     - this violates the rule to keep task changes small and traceable
     - the branch mixes Phase 1 QA history with Phase 2 implementation work

## Verified
- `pytest tests/` passes locally: `8 passed in 0.51s`
- Required project skeleton exists:
  - `app/`
  - `requirements.txt`
  - `alembic/`
- Issue model fields and enums are present.
- `IssueCreate`, `IssueUpdate`, and `IssueResponse` schemas exist.

## Result
- QA result: `FAIL`

## Follow-up
- Created follow-up task: `tasks/SEMI-TASK-015-issue-schema-validation-and-branch-scope-cleanup.md`
