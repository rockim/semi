# QA-2026-04-24-016

## Target
- Task ID: `SEMI-TASK-005`
- Requirement ID: `SEMI-REQ-005`
- Branch: `feature/SEMI-TASK-005-workflow-state-model`
- Reviewed commits:
  - `8639174`
  - `d16dd5e`
- Date: `2026-04-24`
- Reviewer: `Codex QA`

## Findings
1. `Major` `color` is not validated as a hex color code.
   - Files:
     - `requirements/SEMI-REQ-005-workflow-state-model.md:16`
     - `app/schemas/workflow_state.py:12`
   - Detail:
     - the requirement describes `color` as `hex color code`
     - the schema only applies `max_length=7`, so invalid values like `nothex` are accepted
   - Evidence:
     - local check accepted `WorkflowStateCreate(..., color='nothex')`
   - Impact:
     - API-layer validation does not enforce the documented contract for workflow color values
     - invalid display metadata can enter the system and break UI assumptions

2. `Major` `TODO.md` marks `SEMI-TASK-005` as both backlog and completed.
   - File: `TODO.md:6`
   - Detail:
     - `SEMI-TASK-005` appears in the Backlog section and again in the Completed section at `TODO.md:21`
   - Impact:
     - project status is internally inconsistent
     - reviewers and contributors cannot trust the task state summary

## Verified
- `pytest tests/test_workflow_state.py` passes locally: `9 passed in 0.50s`
- `WorkflowState` model fields match the required structure.
- `WorkflowStateCreate`, `WorkflowStateUpdate`, and `WorkflowStateResponse` schemas exist.
- `name` max length is enforced at 100 characters.
- `color` max length is enforced at 7 characters.

## Result
- QA result: `FAIL`

## Follow-up
- Created follow-up task: `tasks/SEMI-TASK-021-workflow-color-validation-and-status-cleanup.md`
